# jarvis-build/requirements.txt — installed into the image via pip.
# Core
websockets>=12.0
psutil>=5.9
# AI
anthropic>=0.40.0
# Voice (optional, heavy — pulled only if JARVIS_FULL_VOICE=1 at build time)
# faster-whisper>=1.0.0
# openwakeword>=0.6.0
# piper-tts>=1.2.0
# Local model (optional)
# llama-cpp-python>=0.2.90
